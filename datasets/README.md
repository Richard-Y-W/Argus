# datasets

Documentation and provenance for every dataset used: source, version/date accessed, known biases (survivorship, backfill), licensing. Raw data blobs stay out of git; document how to re-fetch.
